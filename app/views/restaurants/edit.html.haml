= form_for @restaurant do |f|
  = f.label :name
  = f.text_field :name
  = f.label :description
  = f.text_area :description, :class => "input-area"
  = f.submit 'Update Restaurant',:class => "button"
